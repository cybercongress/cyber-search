apply plugin: 'application'

run {
    systemProperties System.getProperties()
}

dependencies {
    compile project(':core')
}

mainClassName = "fund.cyber.node.connectors.EthereumConnectorApplicationKt"

