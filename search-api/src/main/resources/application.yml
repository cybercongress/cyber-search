management:
  endpoints:
    web:
      expose: ["metrics", "prometheus"]
  metrics:
    export:
      prometheus:
        enabled: true
    binders:
      jvm:
        enabled: true
      logback:
        enabled: false
      uptime:
        enabled: true
      processor:
        enabled: true